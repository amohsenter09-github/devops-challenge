# Prod runtime config (same keys Terraform passes into the container).
# Local Docker without Terraform:
#   docker run --rm -p 8081:8080 --env-file env/prod.env <image>
HELLO_COLOR=green
HELLO_EMOJI=🚀
