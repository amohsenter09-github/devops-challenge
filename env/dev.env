# Dev runtime config (same keys Terraform passes into the container).
# Local Docker without Terraform:
#   docker run --rm -p 8080:8080 --env-file env/dev.env <image>
HELLO_COLOR=blue
HELLO_EMOJI=🛠️
